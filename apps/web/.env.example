NEXT_PUBLIC_API_BASE=https://YOUR-FLOWPILOT-API.vercel.app/api
# Polling is deliberate on Vercel: callbacks and reads can be served by
# different stateless function instances, while MySQL remains the source of truth.
NEXT_PUBLIC_EVENT_TRANSPORT=polling
